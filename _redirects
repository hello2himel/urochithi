# Netlify rewrites for clean username URLs (domain.com/username → index.html)
# Netlify automatically serves static files (.html, .css, .js) and
# /.netlify/functions/* routes BEFORE applying this fallback rewrite.
/*  /index.html  200
